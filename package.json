{
  "name": "monorepo",
  "version": "1.0.0",
  "workspaces": [
    "api",
    "core",
    "database",
    "panel",
    "bot"
  ],
  "scripts": {
    "start:api": "npm start --workspace=api",
    "dev:api": "npm run dev --workspace=api",
    "migrate:db": "npm run migrate --workspace=database"
  }
}
